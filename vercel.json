{
    "version": 2,
    "buildCommand": "apt-get update && apt-get install -y libpq-dev && pip install -r requirements.txt",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "api/app.py"
      }
    ]
  }
  